# dynamic_action_plugin_erp_nested_report
The ERP Nested Grid plugin adds an interactive, nested detail grid (drill-down sub-table) directly inside rows of an Oracle APEX Interactive Report without requiring complex JSON configurations.
